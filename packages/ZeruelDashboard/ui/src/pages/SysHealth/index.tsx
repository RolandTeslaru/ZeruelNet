import React from 'react'

const SysHealth = React.memo(({ show }: { show: boolean }) => {
  return (
    <div id="ZN-Health" className='absolute top-0 left-0 flex flex-row justify-between size-full w-full h-full'>

    </div>
  )
})

export default SysHealth